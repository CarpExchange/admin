/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com'],
  },
  // exportPathMap: '***.html',
  // trailingSlash: true
};

export default nextConfig;
